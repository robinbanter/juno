diff --git a/node_modules/expo-video/build/VideoPlayer.web.js b/node_modules/expo-video/build/VideoPlayer.web.js
index 46dfa83..5a7bbcb 100644
--- a/node_modules/expo-video/build/VideoPlayer.web.js
+++ b/node_modules/expo-video/build/VideoPlayer.web.js
@@ -1,5 +1,25 @@
 import { useMemo } from 'react';
 import resolveAssetSource from './resolveAssetSource';
+
+/**
+ * `HTMLMediaElement.play()` returns a promise that rejects when a pause()
+ * supersedes it (AbortError) or when autoplay with sound is refused
+ * (NotAllowedError). Unhandled, both surface as uncaught exceptions. The first
+ * is a benign race; the second is answered by retrying muted, which browsers
+ * allow.
+ */
+function safePlay(video) {
+    const result = video.play();
+    if (!result || typeof result.catch !== "function")
+        return;
+    result.catch((error) => {
+        if (error && error.name === "NotAllowedError" && !video.muted) {
+            video.muted = true;
+            video.play().catch(() => { });
+        }
+    });
+}
+
 export function useVideoPlayer(source, setup) {
     const parsedSource = typeof source === 'string' ? { uri: source } : source;
     return useMemo(() => {
@@ -228,7 +248,7 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
     }
     play() {
         this._mountedVideos.forEach((video) => {
-            video.play();
+            safePlay(video);
         });
     }
     pause() {
@@ -243,7 +263,7 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
             if (uri) {
                 video.setAttribute('src', uri);
                 video.load();
-                video.play();
+                safePlay(video);
             }
             else {
                 video.removeAttribute('src');
@@ -268,7 +288,7 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
     replay() {
         this._mountedVideos.forEach((video) => {
             video.currentTime = 0;
-            video.play();
+            safePlay(video);
         });
         this.playing = true;
     }
@@ -283,7 +303,7 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
             video.pause();
         }
         else {
-            video.play();
+            safePlay(video);
         }
         video.currentTime = firstVideo.currentTime;
         video.volume = firstVideo.volume;
@@ -309,7 +329,7 @@ export default class VideoPlayerWeb extends globalThis.expo.SharedObject {
             this.playing = true;
             this._mountedVideos.forEach((mountedVideo) => {
                 if (e.target !== mountedVideo) {
-                    mountedVideo.play();
+                    safePlay(mountedVideo);
                 }
             });
         };
